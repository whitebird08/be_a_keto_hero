extends layout

block content

  h1 list of foods
  p objects to tables!
 
  table(border='1')
    thead
      tr
        th Name
        th Pro
        th Fat
        th Cho
        th
          a(href='/food/:id/edit') Edit

    tbody
    //- each item, i in info.all
      tr
        td 
          a(href='/') #{item.meal_name}
        td #{item.pro_ratio}
        td #{item.fat_ratio}
        td #{item.cho_ratio}
    //- tbody
    //-   //- for album in allFoods
    //-     tr
    //-       td
    //-           a(href='/food/' + food._id)= foodname.name
    //-       td
    //-           a(href='/food/' + food._id)= foodname.pro
    //-       td
    //-           a(href='/food/' + food._id)= foodname.fat
    //-       td
    //-           a(href='/food/' + food._id)= foodname.cho

    //-       td
    //-           a(href="/food/" + food._id + '/edit') Edit
    //-        td
            

    //-       td food._id

    //-       extends layout

//- block content

//-   h1 Listing of All People

//-   table(border = '1')
//-       thead
//-         tr
//-           th first_name
//-           th last_name
//-           th date_of_birth

//-       tbody
//-         each item, i in info.all
//-           tr
//-             td 
//-               a(href='/') #{item.first_name}
//-             td #{item.last_name}
//-             td #{item.date_of_birth}